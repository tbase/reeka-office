# Reeka Office Admin

`apps/admin` 是基于 Next.js + shadcn 的后台管理端。

## 当前页面

- `/login`：登录页面（仅 UI，未接入登录 API）
- `/dashboard`：后台首页
- `/cms/services`：CMS 分组下的服务管理页面（字段结构与 `domain-cms` 对齐）

## 启动

```bash
pnpm dev:admin
```
