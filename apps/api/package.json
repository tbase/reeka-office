{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@reeka-office/domain-cms": "workspace:*",
    "@reeka-office/domain-point": "workspace:*",
    "@reeka-office/domain-user": "workspace:*",
    "@reeka-office/jsonrpc": "workspace:*",
    "drizzle-orm": "^0.45.1",
    "mysql2": "^3.18.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
